const initialState = {
    notes: [{value: 'itemText', completed: false, timestamp: '01-01-2001'}],
    selected: null
}

const NotesReducer = (state = initialState, action) => {
    return state
}

export default NotesReducer